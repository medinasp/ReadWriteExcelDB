@model List<ReadWriteExcelSql.Models.ViewModels.ExcelDataViewModel>
@{
    ViewData["Title"] = "Index";
}

@*<h1>Read Files</h1>
<br>
<h4>Formato Texto</h4>
<p>@foreach (var line in ViewBag.LinesText)
    {
        <p>@line</p>
    }
</p>
<br />*@

@*
<h4>Formato Excel</h4>
<p>@foreach (var line in ViewBag.Lines)
    {
            <p>@line</p>
    }
</p>
<br /><br />
<div class="row">
    <form method="post" asp-controller="File" class="text-left form-validate">
        <h3>Excel Importado</h3>
        <table class = "table table-active table-bordered" width="100">
            <thead>
                <tr>
                    <th>Coluna 1</th>
                    <th>Coluna 2</th>
                    <th>Coluna 3</th>
                </tr>
            </thead>
            <tbody>
                @{int x = 0;}
                @foreach (var item in Model)
                {
                    <tr>
                        <td></td>
                           @Html.DisplayFor(modelItem => item.valores)
                        teste
                    </tr>
                }
            </tbody>
        </table>
    </form>
</div>*@

<style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align:center;
  padding: 8px;
}

.mouse:hover {
  background-color: gray; color:white;
}

tr:nth-child(even) {background-color: #f2f2f2;}

</style>

<div class="row">
    <form method="post" asp-controller="File" asp-action="ImportDb" class="text-left form-validate">
        <h3>Excel Importado</h3>
        <input type="hidden" name="totalLinhas" value="@Model.Count" />
        <table>
            <thead style = "background-color: gray; color:white; text-align:center !important">
                <tr>
                    @for (int i = 0; i < Model[0].valores.Count; i++)
                    {
                            <th style = "text-align:center;">Coluna@(i + 1)</th>
                    }
                </tr>
            </thead>
            <tbody>
                @{
                    int ic = 0;
                }
                @foreach (var item in Model)
                {
                        <tr  class="mouse" style="table:table-striped">
                        @for (int i = 0; i < item.valores.Count; i++)
                        {
                                    <td>
                                        <input name="recebida[@ic][@i]" class="form-control" value="@item.valores[i]" />
                                    </td>
                        }
                        </tr>
                    ic++;
                }
            </tbody>
        </table>
        <div class="form-group col-sm-6">
            <button type="submit" class="btn btn-primary">Import Excel File</button>
        </div>
    </form>
</div>


@*<div class="row">
    <form method="post" asp-controller="File" asp-action="ImportDb" class="text-left form-validate">
        <h3>Excel Importado</h3>
        <table>
            <thead style = "background-color: gray; color:white; text-align:center !important">
                <tr>
                    @for (int i = 0; i < Model[0].valores.Count; i++)
                    {
                                <th style = "text-align:center;">Coluna@(i + 1)</th>
                    }
                </tr>
            </thead>
            <tbody>
                @{ int ic = 0;}
                @foreach (var item in Model)
                {
                        <tr  class="mouse" style="table:table-striped">
                        @for (int i = 0; i < item.valores.Count; i++)
                        {
                                   @* <td>@value</td>*@
                                    @*<td><input asp-for="@value" name="Model.ExcelDataViewModel[@ic].valores[]" class="form-control"></td>*@
                                    @*<td><input name="recebida[]" class="form-control" value="@value" /></td>*@
@*                                    <td>
                                        <input name="recebida[@i][@ic]" class="form-control" value="@item.valores[i]" /></td>

                        }
                        </tr>
                }
            </tbody>
        </table>
        <div class="form-group col-sm-6">
            <button type="submit" class="btn btn-primary">Import Excel File</button>
        </div>
    </form>
</div>*@

